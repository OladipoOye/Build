export default function Events() {
    return (
        //Have a box, and h2 on the top, Hstack on bottom
        //have a Hstack which is centred
        // inside the Hstack have 3 boxes of equal size
        //inside the box have each image of appropriate aspect ratio, followed by h2, followed by p
        // style the h2 to maybe have an outline or faint border
        <></>
    );
};