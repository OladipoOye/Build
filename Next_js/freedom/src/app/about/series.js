export default function Series() {
    //explore the chakra tabs section, see what works
    //create tabs, with the content leading to iframes with appropriate aspect ratio
    return (
        <>Yes</>
    );
};